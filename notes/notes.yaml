# Shelf notes — one entry per task/note.
#   human: short line a person reads (required)
#   ai:    verbose, self-contained brief for a future bot with no prior context
#   done:  checkbox state (default false)
# Standard: <shelf>/docs/notes-convention.md
- human: In-app tutorials — Setup, How to Use, Advanced
  done: false
  ai: |
    Build in-app tutorials/guided walkthroughs for new users, in three parts:
    (1) Setup — point the app at the Slippi Playback Dolphin build + Melee ISO +
    output folder (this is what the existing SetupWizard covers; the tutorial
    should frame/explain it). (2) How to Use — the core loop: import .slp/.slpz
    replays (or drag a folder onto the Start screen), build a filter chain
    (Game Filter → Combo Parser → Combo Filter → Sort, or Edgeguards
    Parser→Filter), then play in Dolphin / export MP4s. (3) Advanced — filter
    branching (tree inputs), custom JavaScript filters via the code editor,
    edgeguard metrics/scoring, overlays, performance/thread settings. Surface
    these from the Getting Started panel and Help → Welcome (see
    src/renderer/components/GettingStarted.tsx). Format TBD — could be a guided
    overlay, a docs site, or short embedded videos. Not started; just captured.
- human: Delete SQLite -wal/-shm sidecars at quit so a closed project is one file
  done: true
  ai: |
    Projects are WAL-mode SQLite, so each .lunar has -wal/-shm sidecars; they
    persisted at rest and confused the single-file-document feel. Fix: clean them
    up at app quit, once every connection is released. Two long-lived connections
    hold the file open (main dbConnection + the background DbWorker in dbAsync.ts),
    so the delete must come AFTER both close — and on Windows an open handle blocks
    unlink, so the DbWorker connection must be closed *gracefully*, not abruptly
    terminated. Implementation: DbWorker.ts handles a {__control:'close'} message
    (closes cachedDb, replies 'closed'); dbAsync.shutdownDbWorker() sends it,
    awaits the ack (1.5s cap), then terminates; main.ts before-quit is now async
    (preventDefault → shutdownCleanupAsync → app.exit(0)) with a 3s overall race so
    quit can't hang. shutdownCleanupAsync: capture project path → controller.cleanup
    → await shutdownDbWorker → closeDb (checkpoints WAL to 0 bytes) →
    removeWalSidecars(path). removeWalSidecars is GUARDED: never deletes a -wal with
    size>0 (un-checkpointed data / crash recovery), and any unlink failure is benign
    (left as today). Signals/crashes use the old sync shutdownCleanup (best-effort).
    Only cleans the currently-open project at quit; old leftover sidecars get
    cleaned when those projects are next opened+quit. Verified lint/tsc(127)/jest
    31/31 + main & DbWorker bundles rebuilt — but the actual "quit → folder shows one
    file" behavior needs eyeballing in a running build (esp. Windows).
- human: New-user onboarding — Start screen + Welcome / getting-started
  done: true
  ai: |
    Shipped a conventional first-run experience (VS Code / JetBrains style) so
    Slippi users new to Lunar Clipper feel oriented. Three states: (1) returning
    users still auto-reopen their last project (unchanged); (2) no project open =>
    enriched "Start screen" — the EmptyState in src/renderer/components/Main.tsx,
    now two columns: left Start (New/Open) + Recent, right a "Getting started"
    panel; (3) project open but zero replays => an Import dropzone in the Tray's
    game-filter empty state (src/renderer/components/Tray/Tray.tsx) with an
    [Import replays] button (ipcBridge.importSlpFiles) — live drag-drop already
    works app-wide via Main's handleDrop. New reusable component
    src/renderer/components/GettingStarted.tsx exports GettingStarted (the 1-2-3
    flow + Playback/ISO setup status rows that launch the existing SetupWizard via
    triggerSetupWizard('record') + "Browse Lunar Database" link) and WelcomeModal
    (Help → Welcome overlay, sends/receives the 'showWelcome' channel from
    menu.ts → App.tsx; receive channels need no preload allowlist). Config flag
    showGettingStarted (default-on, treated true when undefined) in defaults.ts +
    types.ts toggles the right column; styles in GettingStarted.css + Main.css
    (.empty-state-col*) + Tray.css (.tray-dropzone*). Also bumped
    VideoManager.detectDolphinPath to probe "Slippi Launcher Beta" and
    %LOCALAPPDATA% in addition to the standard playback path. Deliberately NO
    download/ISO hand-holding (users already have Slippi). Verified: lint clean,
    tsc unchanged at 127, jest 31/31, main bundle rebuilt — but the actual
    rendered UI/spacing still needs eyeballing in a running build.
- human: Surface edgeguard metrics on the Tray clip cards
  done: false
  ai: |
    Every edgeguard clip carries a full `edgeguardMetrics` object (hits, offstageFrames, recoveryFrame, minLedgeDist, blockedByHit, ledgeSteal, edgeguarderOffstage, maxDepthX, minY, score) — see the EdgeguardMetrics type in src/models/methods/edgeguard2.ts — but only `edgeguardScore` is used anywhere (it feeds Sort). The clip cards show none of it. Task: render a compact metrics line on edgeguard clip cards in the Tray (src/renderer/components/Tray/), e.g. "3 hits · 142f offstage · ledge-steal · score 18", gated on the clip having `edgeguardMetrics` so normal clips are unaffected. Purpose: let the user eyeball what a score of 15 vs 30 actually looks like. Not started.
- human: Generate the 650 character-matchup edgeguard filter configs
  done: false
  ai: |
    Goal: a clip set of every character edgeguarding every other character. Melee has 26 characters (Slippi IDs 0-25, Zelda/Sheik counted separately), so 26x25 = 650 ordered (edgeguarder, edgeguardee) pairs — 676 if you include mirrors. Edgeguarding is directional so the pairs are ordered. Plan: one Edgeguards Parser run, then 650 Edgeguards Filter configs each pinned to a (comboerChar, comboeeChar) pair. Don't hand-click 650 of them — generate the filter param objects programmatically. Filter type id is 'edgeguardFilter' (src/models/methods/edgeguardFilter.ts); relevant params are comboerChar / comboeeChar (arrays of Slippi char ids). Char ids are in src/constants/recoveryData.ts and characters. Not started — was offered, user hasn't decided on the generation approach yet.
- human: Tune edgeguard2 detection and scoring
  done: false
  ai: |
    src/models/methods/edgeguard2.ts has known, deliberately-deferred soft spots: (1) `isRecoveryMove` treats any actionStateId >= 341 as a recovery move, which catches ALL character-specific states, not just up-B/side-B — main false- positive risk; a per-character up-B/side-B whitelist would be precise but high-maintenance. (2) The KO gate rejects `death.y >= rects.bz.yMax` as "off the top", which wrongly rejects side-kills near the top corner — could tighten to `death.y >= bz.yMax && Math.abs(death.x) < edge.xMax`. (3) Per-character `recoveryRange` values in src/constants/recoveryData.ts are rough estimates meant to be tuned. (4) Score weights (hits*3 + offstageFrames/30 + edgeguarderOffstage 4 + ledgeSteal 2 + maxDepthX/50 + depthFactor 0-2) are heuristic; effective range ~3-40, 5-15 typical, 20+ = long/deep/multi-hit. All left as-is pending review against real results.
- human: Apply stocks-only computeStocks() to edgeguard.ts too
  done: false
  ai: |
    edgeguard2.ts now computes stocks via computeStocks() (src/lib/slippiStocks.ts) — runs only slippi-js StockComputer instead of the full SlippiGame.getStats() (which also runs combos/conversions/actions/inputs/targets + overall). ~1/3 less post-parse CPU per file, verified byte-identical stocks. The older edgeguard.ts still calls `game.getStats()` then reads `stats.stocks` with the same one-line pattern and would benefit identically. Minor: swap for `computeStocks(game)`.
- human: Cut v2.0.0-beta.4 and smoke-test the pre-release fixes on Windows
  done: false
  ai: |
    ~28 commits of pre-release review fixes sit on local master, UNPUSHED. Push master + tag v2.0.0-beta.4 to trigger .github/workflows/release.yml (builds win+linux, publishes a prerelease with auto-update manifests). This also re-tests the auto-updater: an installed beta.3 should self-update to beta.4 (the first hop that works — beta.2.5 predates the prerelease-aware updater). Then smoke-test what tsc + jest can't verify: (1) custom-code consent gate — running a custom filter pops "Run custom code?"; "Always trust" stops re-prompting; (2) the nodeIntegrationInWorker removal in main.ts — confirm the Tray density/webgl visualization STILL renders (the most likely regression); (3) tightened CSP (index.ejs/codeEditor.ejs) — app loads, no blank screen; (4) data-integrity fixes — import a zero-.slp zip (modal closes, no hang), quit mid-filter-run, Run-All. See RELEASING.md.
- human: Remove or testMode-gate the dev "Reset App" button before GA
  done: false
  ai: |
    src/renderer/components/Top.tsx has a TEMP "Reset App" button (sendMessage 'resetApp') that wipes config to defaults + relaunches with NO confirmation — a data-loss footgun for end users. Intentionally shipped in the beta for Windows testing. Before stable GA, gate it behind {config.testMode && (...)} like the neighboring "Reset SLPZ" button, or delete it. Its own comment says "Remove before GA". Review task #25.
- human: Free Windows code signing via SignPath Foundation before GA
  done: false
  ai: |
    Windows builds ship UNSIGNED (SmartScreen "unknown publisher" nag; auto-update still works unsigned). User won't pay for a cert. SignPath Foundation grants free code-signing certs to qualifying open-source projects (lm-clipper is public). Apply, then add one signing step in release.yml between Package and the checksum step + win.publisherName in package.json; electron-updater's verifyUpdateCodeSignature then passes with no client change. Do before stable GA. See RELEASING.md "Code signing".
- human: Finish breaking up god-objects — big React components + ConfigManager
  done: false
  ai: |
    Review task #35 did the SAFE slice only (extracted config-type migration to src/main/configStore.ts + tests). Deferred because high-churn with UI regression risk tsc/jest can't catch: (1) split the 2331-line CodeEditorPage.tsx and 1761-line FilterControls.tsx into per-panel components; (2) move config STATE ownership out of controller.ts (1700 lines, 68 IPC handlers) into a dedicated ConfigManager (load/merge/save/recent-projects/updateConfig). Do in a dedicated session WITH the app running so each extraction is smoke-tested. NOT pre-beta work.
- human: Single-source the IPC channel allowlist + push Sort into SQL
  done: false
  ai: |
    Review task #38, deferred. (a) The preload SEND_CHANNELS Set (src/main/preload.ts) is hand-maintained and drifts from controller.ts's ipcMain.on registrations — a forgotten channel fails silently ~30s. A new test (src/__tests__/ipcAllowlist.test.ts) now CATCHES drift in CI, so urgency is gone; the clean fix is to derive SEND_CHANNELS from one source of truth shared with the registration loop. (b) The Sort filter (src/models/Worker.ts) loads ALL rows on one thread to sort in JS — won't scale to the ~6M-combo DB; push into SQL ORDER BY with an index. The SQL-sort is a risky rewrite — post-beta.
- human: Pre-release review — data-integrity + custom-code consent gate (MUST fixes)
  done: true
  ai: |
    From a hostile pre-release review, fixed the release-blocking correctness + security issues (committed to local master, beta-4-bound): (1) custom filters run user JS via Node vm (NOT a sandbox; trivially escapable -> RCE on opening+running a shared project) -> consent gate in Filters.tsx + FilterModals.CustomCodeConsentModal + config.approvedCustomCodeHashes; running unapproved custom code prompts Cancel/Run-once/Always-trust keyed by code SHA-256; never auto-runs. (2) quit-time WAL sidecar deletion could lose committed data -> only delete on confirmed-clean shutdown (main.ts). (3) metadata `extra` JSON blob lost-update across two SQLite connections -> read-merge-write under BEGIN IMMEDIATE (db.ts + DbWorker.ts). (4) import hang on zero-slp/skip-zip exits -> always emit finished (ImportManager). (5) Run-All marked crashed-worker partial results as processed -> halt on worker crash (FilterExecutor). (6) closeArchive didn't stop in-flight import/filter/video. See memory custom-code-consent-gate.md. Reset App button gating is a separate open note.
- human: tsc 127 -> 0 errors + CI ratchet typecheck gate
  done: true
  ai: |
    Drove the whole TypeScript backlog to zero (was 127) and gated it. Real fixes, no any/@ts-ignore (only boundary casts at IPC/DB edges). Found real latent bugs: slippi frame.players is an OBJECT keyed by index, not an array (several filters used .filter/.find on it) -> Object.values(...); multiple null derefs. Added @types/better-sqlite3 + a tsconfig path for electron-updater so the DB/updater layers are actually type-checked. Last 7 errors were import.meta: added tsconfig.typecheck.json (extends base, module:esnext) used ONLY by tsc --noEmit, leaving jest/ts-node/webpack on the base commonjs config. Gate: .erb/scripts/tsc-ratchet.js fails if errors exceed .tsc-error-baseline (now 0 = hard gate); CI typecheck job runs it. See memory typecheck-setup.md.
- human: Auto-update pipeline + anonymous usage telemetry + CI hardening
  done: true
  ai: |
    Release-push work alongside the review: (1) rebuilt the release pipeline so published builds actually auto-update — upload latest/beta .yml manifests + blockmaps, publish (not draft), beta/stable channels from the tag, allowPrerelease from version; dropped macOS, retired Conveyor; runbook in RELEASING.md. (2) anonymous usage telemetry: src/main/telemetry.ts (install/app_open/video_created/import_completed/filter_run) -> lunarmelee.com/api/app-usage with a random installId; opt-out + first-run notice; NO consent gate (GDPR legitimate interest). Wire contract + dashboard spec for the website is docs/telemetry-api.md (SERVER MUST DISCARD THE CLIENT IP). (3) CI hardening: npm ci + real prod-dep audit gate (fixed lodash/react-router), release-binary verify step, explicit webPreferences + tighter CSP, serialized/atomic config writes, tests 31 -> 178. See memory release-pipeline.md, usage-telemetry.md.
